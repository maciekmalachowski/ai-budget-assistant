import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Intentionally empty for Phase 1. Extend in later phases
  // (e.g. image domains, headers, rewrites) as needed.
};

export default nextConfig;
